function solution(x,y) {
    let add = x + y;
    let sub = x - y;
    let div = x / y;
    let mul = x * y;

    console.log(add);
    console.log(sub);
    console.log(div);
    console.log(mul);
}

solution(1, 2);
